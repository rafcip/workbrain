# Segreti e dati — MAI in git (i dati veri vivono in /srv/workbrain, fuori dal repo)
env.prod
*.env
.env*
*.key
*.pem
id_rsa*
# eventuali symlink/copie locali dei dati
/raw/
/vault/
/db/
/backups/
data/
# impostazioni locali di Claude Code (per-macchina, non condivise)
# NB: finora erano escluse solo dal gitignore globale di root — invisibile a ogni altro utente/macchina (AUDIT-001 R-12).
.claude/settings.local.json

# rumore
node_modules/
__pycache__/
*.pyc
.venv/
venv/
*.bak-pre-*
.DS_Store
*.log
# scratch
scratch/
tmp/
